<UserControl x:Class="VetManagement.Components.Menus.MedReportsMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VetManagement.Components.Menus"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border Style="{StaticResource FilterMenuSection}" Grid.Row="1">
            <DockPanel >
                <StackPanel Orientation="Horizontal">

                    <StackPanel>
                        <TextBlock Text="Nume medicament" FontSize="13"/>
                        <TextBox Text="{Binding MedName, UpdateSourceTrigger=PropertyChanged}"
                         Width="200"
                         HorizontalAlignment="Left"
                        FontSize="13"/>
                    </StackPanel>

                    <StackPanel  Margin="20,0,0,0">
                        <TextBlock Text="Nume stăpân" FontSize="13"/>
                        <TextBox Text="{Binding OwnerName, UpdateSourceTrigger=PropertyChanged}"
                         Width="200"
                         FontSize="13"
                         HorizontalAlignment="Left"/>
                    </StackPanel>

                    <StackPanel Margin="20,0,0,0">
                        <TextBlock Text="Alegeți o dată" FontSize="13"/>
                        <DatePicker Width="150" SelectedDate="{Binding Date,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                </StackPanel>
                <Border x:Name="ResultsContainer"
                    Background="White"
                    BorderBrush="Gray"
                    BorderThickness="1"
                    Visibility="Collapsed"
                    Width="250">
                    <ListBox ItemsSource="{Binding Meds}"   
        
         Background="White"/>
                </Border>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
