<UserControl x:Class="VetManagement.Components.Menus.UsersMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VetManagement.Components.Menus"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border  Grid.Row="0" Style="{StaticResource ButtonMenuSection}">
            <DockPanel>
                <Button Content="➕ Creează utilizator" Style="{StaticResource BasicButton}" HorizontalAlignment="Left" Background="#FF4CAF50"
            Command="{Binding NavigateCreateUserWindow}" />
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
