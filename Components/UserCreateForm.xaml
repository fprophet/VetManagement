<UserControl x:Class="VetManagement.Components.UserCreateForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:components="clr-namespace:VetManagement.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Create User" FontSize="18" FontWeight="Bold" 
               HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Margin="0,10">
            <TextBlock Text="Username:"/>
            <TextBox Name="Username" Width="250" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,10">
            <TextBlock Text="Name:"/>
            <TextBox Name="Name" Width="250" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,10">
            <TextBlock Text="Email:"/>
            <TextBox Width="250" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Margin="0,10">
            <TextBlock Text="Password:"/>
            <components:BindablePasswordBox Width="250" x:Name="Password" Password="{Binding Password, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" />
        </StackPanel>

        <StackPanel Grid.Row="5" Margin="0,10">
            <TextBlock Text="Role:"/>
            <ComboBox x:Name="Role" Width="250" SelectedItem="{Binding Role, UpdateSourceTrigger=PropertyChanged}" 
                  ItemsSource="{Binding Roles}" />
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <Button Content="Create" Width="100" Margin="5" Command="{Binding CreateUserCommand}" />
            <Button Content="Cancel" Width="100" Margin="5" />
        </StackPanel>
    </Grid>
</UserControl>
