<UserControl x:Class="VetManagement.Views.TreatmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VetManagement.Views"
             xmlns:components="clr-namespace:VetManagement.Components" xmlns:services="clr-namespace:VetManagement.Services"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <services:CommandParameterToIntConverter x:Key="CommandParameterToIntConverter"/>
    </UserControl.Resources>

    <Grid Margin="10,10,0,-143">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Top Bar -->
            <RowDefinition Height="Auto"/>
            <!-- Hidden Form -->
            <RowDefinition Height="*"/>
            <!-- Table -->
        </Grid.RowDefinitions>

        <!-- 🡨 Back Button + Add New Item -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="5">
            <Button Content="🡨 Back" Padding="10,5" Background="#2196F3" Foreground="White"
                FontWeight="Bold" BorderThickness="0" Cursor="Hand"
                Command="{Binding NavigateHomeCommand}" />

            <Button Content="➕ Add New Item" Padding="10,5" Background="#4CAF50" Foreground="White"
                FontWeight="Bold" BorderThickness="0" Cursor="Hand"
                HorizontalAlignment="Right"
                Command="{Binding ToggleFormVisibilityCommand}" />
        </StackPanel>

        <!-- 📋 Hidden Form (Appears When Editing or Adding) -->
        <local:CreateTreatmentView Grid.Row="1" DataContext="{Binding CreateTreatmentViewModel}" Visibility="{Binding isVisibleForm, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <!-- 📋 Inventory Table -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Treatments}" AutoGenerateColumns="False"
              HeadersVisibility="Column" CanUserAddRows="False" CanUserDeleteRows="False"
              Background="White" BorderThickness="1" BorderBrush="#DDD">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Data" Binding="{Binding DateAddedFormated, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="*"/>
                <DataGridTextColumn Header="Nume Proprietar" Binding="{Binding Patient.OwnerName}" Width="100"/>
                <DataGridTextColumn Header="Adresa" Binding="{Binding Patient.OwnerAddress}" Width="120"/>
                <DataGridTextColumn Header="Specie"  Binding="{Binding Patient.Species}" Width="150"/>
                <DataGridTextColumn Header="Rasa" Binding="{Binding Patient.Race}" Width="150"/>
                <DataGridTextColumn Header="Sex" Binding="{Binding Patient.Sex}" Width="150"/>
                <DataGridTextColumn Header="Medicament Administrat" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Cantitate" Binding="{Binding Quantity}" Width="*"/>

                <!-- 🖊 Edit Button Column -->
                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.Header>
                        <TextBlock Text="Actions"/>
                    </DataGridTemplateColumn.Header>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="✏ Edit" Padding="5,2" Background="#FFC107"
                                Foreground="White" BorderThickness="0" Cursor="Hand"
                                Command="{Binding DataContext.EditItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                CommandParameter="{Binding}" />

                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.Header>
                        <TextBlock Text="Actions"/>
                    </DataGridTemplateColumn.Header>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="View" Padding="5,2" Background="Green"
                        Foreground="White" BorderThickness="0" Cursor="Hand"
                        Command="{Binding DataContext.NavigateViewMedCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                        CommandParameter="{Binding Id, Converter={StaticResource CommandParameterToIntConverter}}" />

                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.Header>
                        <TextBlock Text="Actions"/>
                    </DataGridTemplateColumn.Header>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" Padding="5,2" Background="Red"
                        Foreground="White" BorderThickness="0" Cursor="Hand"
                        Command="{Binding DataContext.DeleteMedCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                        CommandParameter="{Binding Id, Converter={StaticResource CommandParameterToIntConverter}}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
