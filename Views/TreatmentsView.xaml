<UserControl x:Class="VetManagement.Views.TreatmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VetManagement.Views" xmlns:services="clr-namespace:VetManagement.Services"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <services:CommandParameterToIntConverter x:Key="CommandParameterToIntConverter"/>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Loading Progress Bar -->
            <ProgressBar Height="15" Width="200" IsIndeterminate="True"
                         Visibility="{Binding isLoading, Converter={StaticResource BoolToVisibilityConverter}}" />

            <!-- Main DataGrid -->
            <DataGrid Grid.Row="1" ItemsSource="{Binding Treatments}"
                      Style="{StaticResource StylishDataGrid}" 
                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                      AutoGenerateColumns="False"
                      Height="Auto" >

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Data" Binding="{Binding DateAddedFormated, StringFormat='yyyy-MM-dd HH:mm:ss'}" Width="150"/>
                    <DataGridTextColumn Header="Nume Animal"  Binding="{Binding Patient.Name}" Width="130" />
                    <DataGridTextColumn Header="Specie"  Binding="{Binding Patient.Species}" Width="100"/>
                    <DataGridTextColumn Header="Rasa" Binding="{Binding Patient.Race}" Width="100"/>
                    <DataGridTextColumn Header="Sex" Binding="{Binding Patient.Sex}" Width="70"/>
                    <DataGridTextColumn Header="Vârsta"  Binding="{Binding Patient.Age}" Width="70"/>
                    <DataGridTextColumn Header="Greutate"  Binding="{Binding Patient.Weight}" Width="100"/>

                    <!-- Nested DataGrid for Medicaments -->
                    <DataGridTemplateColumn Header="Medicamente" Width="403">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="120">
                                    <DataGrid ItemsSource="{Binding TreatmentMeds}" AutoGenerateColumns="False"
                                              HeadersVisibility="Column" CanUserAddRows="False">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Nume" Binding="{Binding Med.Name}" Width="100"/>
                                            <DataGridTextColumn Header="Cantitate" Binding="{Binding Quantity}" Width="100"/>
                                            <DataGridTextColumn Header="Tip Dozaj" Binding="{Binding Med.QuantityType}" Width="100"/>
                                            <DataGridTextColumn Header="Serie Lot" Binding="{Binding Med.LotID}" Width="100"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </ScrollViewer>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Detalii" Binding="{Binding Details}" Width="250"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </ScrollViewer>
</UserControl>
