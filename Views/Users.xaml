<UserControl x:Class="VetManagement.Views.Users"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:VetManagement.Views"
             xmlns:components="clr-namespace:VetManagement.Components">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <!-- Add User Button -->

        <Button Command="{Binding NavigateHomeCommand}" Content="Back" Grid.Row="0" Grid.ColumnSpan="2" HorizontalAlignment="Left" Padding="5,2"/>
        
        <StackPanel Grid.Row="1" Grid.Column="0" Margin="10">
               
            <Button Content="➕ Add New User" Padding="10,5" Background="#4CAF50" Foreground="White" 
                        FontWeight="Bold" BorderThickness="0" Cursor="Hand"
                        Command="{Binding ToggleFormVisibilityCommand}" />
            
            <components:UserList />
            
        </StackPanel>

        <components:UserCreateForm Grid.Row="1" Grid.Column="1" DataContext="{Binding CreateUserViewModel}" Visibility="{Binding isVisibleForm, Converter={StaticResource BoolToVisibilityConverter}}" />
    </Grid>
    
</UserControl>