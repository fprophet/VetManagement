<UserControl x:Class="VetManagement.Views.CreateOwnerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:components="clr-namespace:VetManagement.Components" 
             xmlns:local="clr-namespace:VetManagement.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- Define a colorful style for the button -->
        <Style TargetType="Button" x:Key="CreateOwnerButtonStyle">
            <Setter Property="Background" Value="#FF4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderBrush" Value="#FF388E3C"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Width" Value="200"/>
        </Style>

        <!-- Define a colorful style for TextBoxes -->
        <Style TargetType="TextBox" x:Key="TextBoxStyle">
            <Setter Property="Background" Value="#FFFFFFFF"/>
            <Setter Property="Foreground" Value="#FF333333"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderBrush" Value="#FFBBBBBB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <!-- Define a style for ComboBox -->
        <Style TargetType="ComboBox" x:Key="ComboBoxStyle">
            <Setter Property="Background" Value="#FFFFFFFF"/>
            <Setter Property="Foreground" Value="#FF333333"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderBrush" Value="#FFBBBBBB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <!-- Define a colorful style for ListView -->
        <Style TargetType="ListView" x:Key="OwnerListStyle">
            <Setter Property="Background" Value="#FFEEEEEE"/>
            <Setter Property="Foreground" Value="#FF333333"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="#FFBBBBBB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
        <!-- Define a colorful style for ListViewItem -->
        <Style TargetType="ListViewItem" x:Key="OwnerListItemStyle">
            <Setter Property="Background" Value="#FFFFFFFF"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <DataTrigger Value="True">
                    <DataTrigger.Binding>
                        <Binding Path="IsSelected"/>
                    </DataTrigger.Binding>
                    <Setter Property="Background" Value="#FFD8F9E8"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical" Grid.Column="0" Grid.Row="0" VerticalAlignment="Top" Margin="10,10,10,70" Width="500">
            <!-- First Section: Owner's Basic Info -->
            <TextBlock Text="Detaliile stăpânului" FontSize="18" FontWeight="Bold" Margin="0,10"/>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Vertical" Grid.Row="0">
                    <TextBlock Text="Nume:" VerticalAlignment="Center" Margin="5"/>
                    <TextBox Text="{Binding Name}" Name="NameTextBox" Style="{StaticResource TextBoxStyle}" />
                </StackPanel>
                <StackPanel Orientation="Vertical" Grid.Row="1">
                    <TextBlock Text="Adresa:" VerticalAlignment="Center" Margin="5" />
                    <TextBox Text="{Binding Address}" Name="AddressTextBox" Style="{StaticResource TextBoxStyle}" Grid.Row="1"/>
                </StackPanel>
                <StackPanel Orientation="Vertical" Grid.Row="2">
                    <TextBlock Text="Telefon:" VerticalAlignment="Center" Margin="5" />
                    <TextBox Text="{Binding Phone}" Name="PhoneTextBox" Style="{StaticResource TextBoxStyle}" Grid.Row="2"/>
                </StackPanel>
                <StackPanel Orientation="Vertical" Grid.Row="3">
                    <TextBlock Text="Email:" VerticalAlignment="Center" Margin="5" />
                    <TextBox Text="{Binding Email}" Name="EmailTextBox" Style="{StaticResource TextBoxStyle}" Grid.Row="3"/>
                </StackPanel>
                <StackPanel Orientation="Vertical" Grid.Row="4">
                    <TextBlock Text="Detalii:" VerticalAlignment="Center" Margin="5" />
                    <TextBox Text="{Binding Details}" Name="DetailsTextBox" Style="{StaticResource TextBoxStyle}" Grid.Row="4" Height="100"/>
                </StackPanel>
                <Button Content="Create Owner" HorizontalAlignment="Center" VerticalAlignment="Center"
                Style="{StaticResource CreateOwnerButtonStyle}" Command="{ Binding CreateOwnerCommand}" VerticalContentAlignment="Center" 
                Grid.Row="5"/>
            </Grid>
        </StackPanel>

        <components:CreatePatientForm Grid.Column="1" DataContext="{Binding CreatePatientViewModel}" />

    </Grid>
</UserControl>
